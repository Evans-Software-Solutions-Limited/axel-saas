services:
  openclaw:
    build: .
    ports:
      - "18789:18789"
    environment:
      # Pick ONE auth mode. Token is easiest.
      OPENCLAW_GATEWAY_TOKEN: "change-me-to-a-long-random-string"
      # Optional if you need a non-default port:
      OPENCLAW_GATEWAY_PORT: "18789"
