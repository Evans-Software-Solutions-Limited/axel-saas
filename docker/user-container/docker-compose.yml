services:
  openclaw:
    build: .
    ports:
      - "18789:18789"
    environment:
      OPENCLAW_GATEWAY_TOKEN: "dev-token-change-me"
      TIER: "starter"
    volumes:
      - ./workspace-test:/data/workspace
