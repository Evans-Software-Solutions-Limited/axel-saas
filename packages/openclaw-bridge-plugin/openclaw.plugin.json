{
  "id": "axel-bridge",
  "name": "Axel Gateway Bridge",
  "description": "Exposes Axel SaaS's gateway-contract REST surface. Translates Axel's POST /api/chat (and matching health/reload/usage endpoints) to OpenClaw's first-party /v1/chat/completions and runtime APIs.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "chatCompletionsBaseUrl": {
        "type": "string",
        "description": "Internal URL the bridge calls to reach OpenClaw's /v1/chat/completions endpoint. Defaults to http://127.0.0.1:18789 — the loopback address of the gateway hosting this plugin. Override only for unusual local-test setups.",
        "default": "http://127.0.0.1:18789"
      },
      "defaultModel": {
        "type": "string",
        "description": "Model id passed to /v1/chat/completions when the request body omits one. Falls back to the gateway's configured agent default if absent on both sides.",
        "default": "anthropic/haiku"
      }
    }
  }
}
